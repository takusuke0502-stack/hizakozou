# エージェント指示の見直し記録

確認日: 2026-09-13。対象は hizakozou-main の AGENTS.md、CLAUDE.md、ブログ案内、読みやすさ改修指示書、ローカルの GitNexus スキル6件。個人共通スキルとプラグイン配布物は変更していない。

## 根拠と変更

- [OpenAIの記事](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)を確認。短く明確なスキル説明、必要時だけの資料参照、過剰な固定手順の削減、依頼ごとの完了条件という方針を適用した。記事からモデル固有の設定や性能向上を推定してはいない。
- Codexの「3〜5位の仕様書を作成」（タスクID `019f381a-cff3-7f90-9346-6beffc87ca99`）の直近の会話で、PCの横幅活用、スマホ構成の維持、目次と1枚の説明画像という指定を確認。幅・列数・画像構成の目安が新しい指定を妨げないよう修正した。このページ固有の1200pxや画像枚数を全ページの規則にはしていない。
- このプロジェクトのClaudeセッション `fefbeb07-a61e-41a6-bc16-7a89c0ef9624` では、説明と画像の不一致、古い室内写真の差し替え依頼を確認。画像の実見と説明との照合を追加した。
- Claudeセッション `f55c4434-ee91-486e-a845-83ca13e83602` と `72630318-10aa-4a31-849d-0b2749b73b46` では、大幅変更を避けること、清潔感・信用性、患者と家族の読みやすさを確認。既存の採用部分と今回の変更範囲を尊重する指示にした。過去のTailwind推奨や計画提示を恒久的な必須手順にはしていない。
- 現行ファイルではCLAUDE.mdにGitNexus規則が重複し、スキルではWorkflowとChecklistが重複していた。共有規則をAGENTS.mdにまとめ、各スキルは必要な調査を選べるよう整理した。
- 既存の `1記事プッシュ` の選択・生成・確認・公開という契約を保持。通常の実装と提案・プレビューの完了条件を区別した。

## 検証と限界

- GitNexusスキル6件すべてが skill-creator の `quick_validate.py` に合格。
- 変更した案内とスキルのローカルMarkdownリンクは存在を確認。
- 既存の管理対象・未追跡ファイル1192件の作業開始時ハッシュと比較し、今回の案内ファイル以外に変更がないことを確認。Git管理対象外のスキル6件は別途検証した。
- 指示だけの変更のため、サイト生成・サイトテストは行っていない。実際の次回タスクでの成功率や速度改善はまだ未検証。
- 見直し後のユーザーの「プッシュ」指示により、この指示書の更新を公開対象にした。スキルの格納先 `.claude/skills/gitnexus/` は既存のGit除外対象だが、今回の6つの `SKILL.md` を個別に追跡対象へ追加する。Git除外設定や他のローカル設定は変更しない。
- 履歴は関連するCodexタスクと当該プロジェクトのClaudeセッションを確認した範囲。全プロジェクト・全会話の網羅調査ではない。

## 次の実作業で確かめること

| 依頼例 | 期待する判断 |
|---|---|
| 指定した一文だけを直す | 全リポジトリの解析や無関係な設計変更を始めない。コラム本文ならソースを変更して生成する |
| PCの横幅を使いたい | 本文の一列の読み幅とページ全体の幅を区別し、スマホも確認する |
| 1記事プッシュ | 対象1件と必要な生成物を検証し、無関係な差分を混ぜず公開まで進める |
| 共通関数を変更する | 影響分析と該当動作の検証を行い、グラフの欠落を影響なしと扱わない |
| 変更案を提案して | 提案を返す。履歴に実装・公開があるだけで今回の権限とみなさない |

この表は次回評価用であり、独立したエージェントによる動作試験を実施済みという意味ではない。
